{
  "name": "tv-crm-bot-server",
  "version": "1.0.0",
  "description": "TV CRM — WebSocket + Telegram Bot + Gemini AI Server",
  "main": "server.js",
  "scripts": {
    "start":   "node server.js",
    "dev":     "nodemon server.js --watch server.js --watch .env",
    "install": "npm install"
  },
  "dependencies": {
    "@google/generative-ai":   "^0.21.0",
    "dotenv":                  "^16.4.5",
    "node-telegram-bot-api":   "^0.65.1",
    "ws":                      "^8.18.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
